# Commands

Install dependencies.
 ```bash
 npm install
 ```

Start dev server at http://localhost:3000.
 ```bash
 npm run dev
 ```

Run tests
 ```bash
 npm run test
 ```

Get tests coverage
 ```bash
 npm run coverage
 ```
